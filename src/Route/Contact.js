// @flow strict
import * as React from 'react';

/**
 * Contact react component.
 *
 * @type {Contact}
 * @extends React.Component
 */
export default class Contact extends React.Component <{}> {

  /**
   * Render component.
   *
   * @type {React.Node}
   */
  render = (): React.Node => {
    return (
         <p className="text-center">Contact page is coming soon.</p>
      );
  }
}
